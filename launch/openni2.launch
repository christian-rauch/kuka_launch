<launch>
  
  <!-- launch openni2 with depth registration -->
  <include file="$(find openni2_launch)/launch/openni2.launch">
    <arg name="depth_registration" value="true" />
  </include>
  
  <node pkg="apriltags_ros" type="apriltag_detector_node" name="apriltag_detector" output="screen">
    <!-- Remap topic required by the node to custom topics -->
    <remap from="image_rect" to="camera/rgb/image_rect_color" />
    <remap from="camera_info" to="camera/rgb/camera_info" />

    <!-- Optional: Subscribe to the compressed stream-->
    <param name="image_transport" type="str" value="compressed" />

    <!-- Select the tag family: 16h5, 25h7, 25h9, 36h9, or 36h11(default) -->
    <param name="tag_family" type="str" value="16h5" />

    <!-- Enable projected optical measurements for more accurate tag transformations -->
    <!-- This exists for backwards compatability and should be left true for new setups -->
    <param name="projected_optics" type="bool" value="true" />

    <!-- Describe the tags -->
    <rosparam param="tag_descriptions">[
      {id: 0, size: 0.147, frame_id: t0},
      {id: 1, size: 0.147, frame_id: t1},
      {id: 2, size: 0.147, frame_id: t2},
      {id: 3, size: 0.147, frame_id: t3}]
    </rosparam>
  </node>

  <!-- Transformation from tag t0 to robot base -->
  <node pkg="tf" type="static_transform_publisher" name="t0_transform" args="-0.255 -0.255 0 0 0 0 1 t0  world_frame 100" />
</launch>
